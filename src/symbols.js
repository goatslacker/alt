const BATCHED_STATE = Symbol('__batchedState')
const FLUSH_STATE = Symbol('__flushState')
const TRANSMITTER = Symbol('__transmitter')

module.exports = {
  BATCHED_STATE,
  FLUSH_STATE,
  TRANSMITTER,
}
